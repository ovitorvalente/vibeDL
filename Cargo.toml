[package]
name = "vibeDL"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'

[dependencies]
clap = { version = "4.5", features = ["derive"] }
